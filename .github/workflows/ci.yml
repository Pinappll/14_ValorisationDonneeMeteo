name: CI/CD

on:
  push:
    branches:
      - "**"
  pull_request:

permissions:
  contents: read

jobs:
  backend:
    name: Backend checks
    runs-on: ubuntu-24.04

    defaults:
      run:
        working-directory: backend

    services:
      timescaledb:
        image: timescale/timescaledb:2.25.0-pg17
        env:
          POSTGRES_USER: ${{ secrets.DB_USER }}
          POSTGRES_PASSWORD: ${{ secrets.DB_PASSWORD }}
          POSTGRES_DB: ${{ secrets.DB_NAME }}
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U infoclimat -d meteodb"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          version: "0.10.0"

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          uv sync --extra dev
          cp .env.example .env

      - name: Run linter (ruff)
        run: uv run ruff check .

      - name: Run unit tests
        run: |
          uv run pytest weather/tests/unit/ \
            --junitxml=pytest-unit-report.xml \
            -v

      - name: Run integration tests
        env:
          DB_HOST: localhost
          DB_PORT: 5432
          DB_NAME: ${{ secrets.DB_NAME }}
          DB_USER: ${{ secrets.DB_USER }}
          DB_PASSWORD: ${{ secrets.DB_PASSWORD }}
          MOCKED_DATA: "false"
        run: |
          uv run pytest weather/tests/integration/ \
            --junitxml=pytest-integration-report.xml \
            -v

      - name: Run dependency security scan (pip-audit)
        run: |
          uvx pip-audit -f json -o pip-audit-report.json || true

      - name: Upload backend reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: backend-reports
          path: |
            backend/pytest-unit-report.xml
            backend/pytest-integration-report.xml
            backend/pip-audit-report.json

  frontend:
    name: Frontend checks
    runs-on: ubuntu-24.04

    defaults:
      run:
        working-directory: frontend

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js 24
        uses: actions/setup-node@v6
        with:
          node-version: "24"
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        run: npm ci --legacy-peer-deps

      - name: Run tests
        run: npm run test:ci

      - name: Run linter (eslint)
        run: npm run lint

      - name: Build frontend
        run: npm run build

  docker:
    name: Build and scan ${{ matrix.service }} Docker image
    runs-on: ubuntu-24.04
    needs: [backend, frontend]
    permissions:
      contents: read
      packages: write

    strategy:
      matrix:
        service: [backend, frontend]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build ${{ matrix.service }} Docker image
        run: |
          docker build \
            -t ghcr.io/${GITHUB_REPOSITORY,,}-${{ matrix.service }}:${GITHUB_SHA} \
            ./${{ matrix.service }}

      - name: Install Trivy
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y wget gnupg lsb-release
          wget -qO - https://aquasecurity.github.io/trivy-repo/deb/public.key | \
            gpg --dearmor | sudo tee /usr/share/keyrings/trivy.gpg > /dev/null
          echo "deb [signed-by=/usr/share/keyrings/trivy.gpg] \
            https://aquasecurity.github.io/trivy-repo/deb \
            $(lsb_release -sc) main" | \
            sudo tee /etc/apt/sources.list.d/trivy.list
          sudo apt-get update -qq
          sudo apt-get install -y trivy

      - name: Scan ${{ matrix.service }} image with Trivy
        run: |
          trivy image \
            --format json \
            --output trivy-${{ matrix.service }}-report.json \
            --severity CRITICAL,HIGH \
            --exit-code 0 \
            ghcr.io/${GITHUB_REPOSITORY,,}-${{ matrix.service }}:${GITHUB_SHA}

      - name: Upload Docker scan report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: docker-${{ matrix.service }}-scan-report
          path: trivy-${{ matrix.service }}-report.json

  push-image:
    name: Push ${{ matrix.service }} image to GHCR
    runs-on: ubuntu-24.04
    needs: [docker]
    if: github.ref == 'refs/heads/main'
    permissions:
      contents: read
      packages: write

    strategy:
      matrix:
        service: [backend, frontend]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push ${{ matrix.service }} image
        run: |
          docker build \
            -t ghcr.io/${GITHUB_REPOSITORY,,}-${{ matrix.service }}:${GITHUB_SHA} \
            -t ghcr.io/${GITHUB_REPOSITORY,,}-${{ matrix.service }}:latest \
            ./${{ matrix.service }}
          docker push ghcr.io/${GITHUB_REPOSITORY,,}-${{ matrix.service }}:${GITHUB_SHA}
          docker push ghcr.io/${GITHUB_REPOSITORY,,}-${{ matrix.service }}:latest

      - name: Upload VEX file
        if: matrix.service == 'backend' # ← upload une seule fois
        uses: actions/upload-artifact@v4
        with:
          name: vex-file
          path: security/trivy.vex.json
